# Add Two Numbers

Functions let you package logic into reusable blocks.

In this lesson, you'll write a small helper function for your game engine.

## Assignment

Create a function named `add(a, b)` that returns the sum of the two arguments.

## Requirements

- Do not print anything.
- Return the computed value directly.
- The function should work with positive and negative integers.
